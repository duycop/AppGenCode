'use strict';
let lib = {};
export function set_lib(L) { lib = L; }

const api = '/api/';
function fix_dialog() {
	$('.jconfirm-holder').width($('.jconfirm-open').width());
}
export function main() {
}

